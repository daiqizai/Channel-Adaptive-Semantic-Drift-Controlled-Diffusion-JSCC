phase: S6
analysis_id: exp_s4_006_alpha_head_residual_refiner_tail_regression_benefit
method: BenefitAwareTailContinuousAlphaResidualRefiner
dataset: COCO2017
image_size: 256
channel: AWGN
snrs: [1, 4, 7, 13, 19]
cbr: 0.17
seed: 42
repository_url: https://github.com/daiqizai/Channel-Adaptive-Semantic-Drift-Controlled-Diffusion-JSCC.git

inputs:
  base_refiner_checkpoint: outputs/EXP-S4-006/checkpoints/best.pt
  adaptive_per_sample_csv: outputs/analysis/exp_s4_006_adaptive_residual_alpha_policy/per_sample.csv
  adaptive_summary_csv: outputs/analysis/exp_s4_006_adaptive_residual_alpha_policy/summary.csv
  two_stage_summary_csv: outputs/analysis/exp_s4_006_two_stage_residual_alpha_policy/summary.csv
  benefit_features_csv: outputs/analysis/exp_s4_006_benefit_alpha_predictor/features.csv

splits:
  train_split: validation
  eval_splits: [validation, held-out, test-like]

source_policy:
  target: adaptive_max_top1_consistent_alpha
  target_mode: benefit_utility_alpha
  benefit_target_field: utility_target_alpha
  full: top1_full_strength
  m0: m0

alphas: [0.0, 0.25, 0.5, 0.75, 1.0]

model:
  name: alpha_head_residual_refiner_tail_regression_benefit
  alpha_mode: regression
  base_channels: 48
  num_blocks: 5
  snr_norm_max: 20.0
  residual_gates:
    "1": 0.12
    "4": 0.10
    "7": 0.08
    "13": 0.05
    "19": 0.04
  freeze_refiner: false
  trainable_refiner_parts: [tail]
  alpha_head_hidden: 64
  alpha_head_dropout: 0.05

training:
  epochs: 160
  batch_size: 16
  num_workers: 4
  lr: 0.001
  refiner_lr: 0.00005
  weight_decay: 0.00001
  grad_clip_norm: 1.0
  ce_weight: 0.0
  alpha_loss_weight: 0.20
  regression_loss: smooth_l1
  regression_beta: 0.10
  class_weighting: none
  soft_refiner_detach: false
  soft_mse_weight: 25.0
  soft_l1_weight: 0.0
  target_alpha_mse_weight: 25.0
  target_alpha_l1_weight: 0.0
  full_mse_weight: 100.0
  full_l1_weight: 0.0
  validation_every_epochs: 10

classifier:
  backend: torchvision
  model_name: alexnet
  weights: IMAGENET1K_V1
  cache_dir: outputs/cache/torch
  weights_file: outputs/cache/torch/hub/checkpoints/alexnet-owt-7be5be79.pth
  batch_size: 32
  topk: 5

evaluation:
  image_batch_size: 16
  sample_grid_count: 4
  metrics:
    - psnr
    - ssim
    - ms_ssim
    - pseudo_final_failure
    - accept_rate
    - utility_target_accuracy
    - continuous_alpha
  lpips: omitted_to_avoid_weight_loading_or_download

notes:
  - This follow-up tests whether continuous alpha regression fixes the discrete alpha-head tendency to ignore alpha=0.25.
  - Head and body features stay frozen; only the residual tail and continuous alpha head are trainable.
  - The alpha target is the same benefit utility alpha target used in the frozen, joint, and tail-only classification runs.
  - The loss remains reconstruction-dominant to protect the EXP-S4-006 restoration anchor.
  - Evaluation uses the continuous predicted alpha image, then applies the same frozen AlexNet top-1 fallback.

outputs:
  output_dir: outputs/analysis/exp_s4_006_alpha_head_residual_refiner_tail_regression_benefit
